# Phase 1

Hello hope you're doing great today, I want us to continue working on the dashboard
- Today we'll be taking care of the super admin dahboard to its last ends, editting everythingm that I don't like, I want to make it atractive to my clients
- I've got some other dashboard and UI components that some are messy but lets start with admin dashboard
# Openion
I want you to look at the admin dashboard I created before creating the dashboard app, this is found in components/accounts/admin in browser it is so clean and perfect and very attractive, read it whole and all the modules that the components are importing 
Now I builded the dashboard app and now I integrated the admin dashboard but it doesn't give satisfaction, it just look bold and unattractive I'de prefer we implement it the way I did with the other dashboard of the accounts: Location of this new super_admin dashboard is in components/dashboard/pages/SuperAdminDashboard/** with its layouts mainly in the components/dashboard/layout and the sidebar components in components/dashboard/Sidebar
For the earlier dashboard the layouts and sidebar are in components/common/layout/**

# What you need to do
1. read the accounts admin dashboard and understad the whole logic of the code though some still don't have real-data, though tenants and users data were real-data calling it caunts the exact number of the user in it and tenants 
2. Now the dashboard app super-admin code analyzation, though I don't want most of them
3. read the initial sidebar in components/common/layout to see how i was imp-lementing the other apps elements and components, now for this nmew dashboard the sidebar in components/dashboard/Sidebar/SuperAdminDashboard and all the other sidebar the only components that when click the layout remains the way it is is the dashboard app elements, the rest when clicked the page opens full browser not leaving the sidebar components making it if you want to go to any element you must undo first to get back to the home/overview so that you can see the sidebar but the other one was just good, working perfectly for all the other elements
4. I realy want the layout of this new super-admin dashboard to look like the one for accounts component admin
5. I saqw t5he new dashboard have the system-overview, don't mind it because it is not even real-data, you see there is that app called configs and its files in frontend are components/config, services/config, stores/config etc the configs app is the overal system config etc, I want the system overview to come from there al;ready I have the config dfashboard page and components
6. Now just read the new dashboard app super-admin dashboard files, maybe we can start changing from backend
7. I don't want you to edit the new dashboard app, just write the new ones I'll integrate them later, make sure real-data, real-time all are integrated well, but mostly I think we can just use the exact dashboard from accounts app for tghe super-admin it is very clear, lemme compare how yours will look like and the ones I have 
## Lets begin, make me proud 

# Phase 2:

