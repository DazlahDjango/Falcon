Now ai assume we've handled everything in those two files that I've requested the accounts.md and the reports.md now on the same but now the frontend of each, I know you have the memory of what we've done

now this are the accounts frontend files
from frontend/src:
config/constants/accountsApiConstants and accountsRoutesConstants
services/accounts/api/**(for reporting I think we need to create new)
store/accounts/slice/**(for reporting)
store/accounts/selectors/** also creating the reporting one
hooks/accounts/** also creation of the reporting
components/accounts/** also creating the ones reporting if needed or editing the reports/ directory
pages/accounts/**
routes/accounts.routes.jsx
config/navigation/platformAdminNav