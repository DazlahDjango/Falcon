Now I've tried my best ajnd fixed all the errors and exceptions that existed and now I'm upto styling, Everything is running great, muchly as I expected and great the only thing I'm missing is the dashboard layout coploring and the layout styles.
## Requirements
- You'll read the dashboard layout files, the main layout's css file is in components/layout.css, the other layout css files are in the style/dashboard. I'll call them in the index.css
- Expectations, I wanted my dashboard to look like the one in the frontend/src/assets/images/layout/desktop.png, mobile.png for mobile devices and the icon-s.png which is the falcon icon
- I wanted the blue color for the sidebar components like the one in those images, i hope we can achieve that

## Tasks
1. Make sure all the icons in the dashboard app components, pages and any other dashboard app file that has some strange icons are react-icons ie(fi-icons, md-icons, hi2, b2 etcetera)
2. I'll share with you some picture that I want the dashboard app to look like but first handle the first task