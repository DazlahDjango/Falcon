<<<<<<< HEAD
# .devcontainer/docker-compose.yml
version: '3.8'

services:
  app:
    build:
      context: ..
      dockerfile: .devcontainer/Dockerfile
    volumes:
      - ..:/workspaces/falcon-pms:cached
      - falcon-postgres-data:/var/lib/postgresql/data
    environment:
      - DJANGO_ENV=development
      - PYTHONUNBUFFERED=1
    network_mode: service:db
    command: sleep infinity

  db:
    image: postgres:15
    restart: unless-stopped
    volumes:
      - falcon-postgres-data:/var/lib/postgresql/data
    environment:
      POSTGRES_DB: falcon_pms
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: postgres
    ports:
      - "5432:5432"
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U postgres"]
      interval: 5s
      timeout: 5s
      retries: 5

  redis:
    image: redis:7-alpine
    restart: unless-stopped
    ports:
      - "6379:6379"
    volumes:
      - falcon-redis-data:/data
    healthcheck:
      test: ["CMD", "redis-cli", "ping"]
      interval: 5s
      timeout: 5s
      retries: 5

volumes:
  falcon-postgres-data:
=======
# .devcontainer/docker-compose.yml
version: '3.8'

services:
  app:
    build:
      context: ..
      dockerfile: .devcontainer/Dockerfile
    volumes:
      - ..:/workspaces/falcon-pms:cached
      - falcon-postgres-data:/var/lib/postgresql/data
    environment:
      - DJANGO_ENV=development
      - PYTHONUNBUFFERED=1
    network_mode: DB_HOST=db
    command: sleep infinity

  db:
    image: postgres:15
    restart: unless-stopped
    volumes:
      - falcon-postgres-data:/var/lib/postgresql/data
    environment:
      POSTGRES_DB: falcon_pms
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: postgres
    ports:
      - "5432:5432"
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U postgres"]
      interval: 5s
      timeout: 5s
      retries: 5

  redis:
    image: redis:7-alpine
    restart: unless-stopped
    ports:
      - "6379:6379"
    volumes:
      - falcon-redis-data:/data
    healthcheck:
      test: ["CMD", "redis-cli", "ping"]
      interval: 5s
      timeout: 5s
      retries: 5

volumes:
  falcon-postgres-data:
>>>>>>> d90f1917d5924b006e3d600e78eae230e532a2cc
  falcon-redis-data: