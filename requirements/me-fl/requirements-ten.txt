# ============================================================================
# FALCON PMS - TENANT MODULES (Working versions)
# ============================================================================

# ----------------------------------------------------------------------------
# MULTI-TENANCY (Working versions from your install)
# ----------------------------------------------------------------------------
django-multitenant==4.1.1      # Successfully installed
django-rls==0.4.1              # Latest version (0.4.1 instead of 1.0.0)
django-pgschemas==1.1.1        # Successfully installed

# ----------------------------------------------------------------------------
# SECURITY & AUTHENTICATION
# ----------------------------------------------------------------------------
django-guardian==3.3.0          # Successfully installed
django-otp==1.7.0               # Successfully installed
qrcode==7.4.2                   # REPLACEMENT for django-otp-qrcode
pillow==10.3.0                   # For QR image generation (already have)

# ----------------------------------------------------------------------------
# KPI ENGINE (All working)
# ----------------------------------------------------------------------------
django-aggregate-if==0.5
django-pivot==1.10.0
django-mptt==0.18.0

# ----------------------------------------------------------------------------
# WORKFLOW (All working)
# ----------------------------------------------------------------------------
django-fsm==3.0.1
django-viewflow==2.2.15
django-workflows==1.1.1

# ----------------------------------------------------------------------------
# NOTIFICATIONS (All working)
# ----------------------------------------------------------------------------
django-notifications-hq==1.8.3
django-apscheduler==0.7.0

# ----------------------------------------------------------------------------
# REPORTING (All working)
# ----------------------------------------------------------------------------
reportlab==4.4.10
openpyxl==3.1.5
python-pptx==1.0.2
weasyprint==68.1
django-easy-pdf==0.1.1

# ----------------------------------------------------------------------------
# API DOCUMENTATION (All working)
# ----------------------------------------------------------------------------
drf-yasg==1.21.15
drf-spectacular==0.29.0

# ----------------------------------------------------------------------------
# HIERARCHY (All working)
# ----------------------------------------------------------------------------
django-tree-queries==0.23.1